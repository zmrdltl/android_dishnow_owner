<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_violet</item>
        <item name="colorPrimaryDark">#000000</item>
        <item name="colorAccent">@color/color_red</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="colorPrimary">@color/color_violet</item>
        <item name="colorPrimaryDark">#000000</item>
        <item name="colorAccent">@color/color_violet</item>
        <item name="editTextColor"></item>
    </style>

    <style name="SignupActionBar" parent="Theme.AppCompat.Light.NoActionBar">

    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
